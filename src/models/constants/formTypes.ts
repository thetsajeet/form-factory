import { FormElementTypes } from "../interfaces/FFElements";

export const FORM_TYPES: { [key: string]: FormElementTypes } = {
  TEXT: "text",
  PASSWORD: "password",
  TEXTAREA: "textarea",
  SELECT: "select",
  EMAIL: "email",
  NUMBER: "number",
  URL: "url",
};
